streamlit>=1.35.0

# ── spaCy: DEVE essere >=3.8.0 ───────────────────────────────────────────────
# spaCy 3.7.x usa thinc 8.2.x compilato contro numpy 1.x.
# Con numpy 2.x (default attuale) si ottiene:
#   ValueError: numpy.dtype size changed, may indicate binary incompatibility
# spaCy 3.8.0+ usa thinc 8.3+ che supporta numpy 2.x in modo nativo.
spacy>=3.8.0

# ── numpy: floor esplicito per evitare downgrade indesiderati ────────────────
numpy>=1.26.4

# ── Modello italiano con word vectors (OBBLIGATORIO per similarity()) ────────
# Eseguire DOPO pip install, oppure lasciare che setup.sh lo faccia:
#   python -m spacy download it_core_news_md
# NB: it_core_news_sm NON ha word vectors statici → similarity() = 0.
#     Usare md (>=43 MB) o lg.

requests
Pillow
